[package]
name = "dog_essentials"
version = "0.1.0"
edition = "2024"

[dependencies]
k_corelib = {path = "../../k_corelib" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[lib]
crate-type = ["staticlib", "rlib"]